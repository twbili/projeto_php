{% extends 'base.html.twig' %}

{% block title %}Criar novo produto{% endblock %}

{% block body %}
    <h1>Inserir novo produto</h1>

    {{ include('admin/product/_form.html.twig') }}
    <div class="text-end">
        <a class="btn btn-primary float-start me-3 mb-5" href="{{ path('app_admin_product_index') }}">Retornar</a>
    </div>
{% endblock %}
