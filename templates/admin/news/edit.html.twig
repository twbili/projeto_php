{% extends 'base.html.twig' %}

{% block title %}Edit News{% endblock %}

{% block body %}
    <h1>Editar Noticias  </h1>

    {{ include('admin/news/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('admin/news/_delete_form.html.twig') }}
    <div class="row text-end">
        <div class="col text-end">
            <a class="btn btn-primary float-start me-3 mb-5 text-end" href="{{ path('app_admin_news_index') }}">Retornar</a>
        </div>
    </div>
{% endblock %}
